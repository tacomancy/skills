{
  "name": "@tacomancy/skills",
  "private": true,
  "description": "Agent skills distilled from how tacomancy projects are run. The skills are the source; this package exists only to test them.",
  "scripts": {
    "test": "tsc --noEmit && vitest run"
  },
  "packageManager": "pnpm@10.4.1",
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "yaml": "^2.8.1"
  }
}
