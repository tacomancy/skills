# Guidance file — no post-merge section

The `CLAUDE.md` of `acme/reader`, the repository in `../ticket-set`, with no section stating what must happen when a change lands: project, tests, style, and nothing else. There is no `AGENTS.md`. Read beside the ticket set's PR #12; the diff is the one `../guidance-with-triggers/README.md` gives.
