# Skill inventory — partial

`ls .agents/skills/` in `acme/reader`:

```
bluesky-feature-pipeline
```

Only the pipeline is installed. `install.sh` has run as in `skills-full.md`: templates, workflows, and labels exist with the family prefix `feature`. No grill skill, no `guidance-tiers`, `reversibility-rank`, `pin-prototypes`, `wayfinder`, `to-spec`, `handoff`, `to-tickets`, `implement`, `tdd`, `code-review`, `diagnosing-bugs`, or `land-ticket`.
