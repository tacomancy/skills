# NNNN: <the decision, as a sentence>

**Status:** Proposed — becomes Accepted when <a checkable gate: a prototype issue's checklist, a measurement; use Accepted when nothing is pending, Superseded by NNNN when a later ADR replaces this one>

<What this ADR closes — an earlier ADR's deferral, an open item in the architecture document, a brief's open question — and the decision in prose, so the chain of reasoning reads without dates. Ends with the sentence that names the section of the architecture document holding the detail this ADR leaves reversible.>

## Decisions

1. **<Lead phrase of the first decision.>** <The reasoning, inline: why this and not the nearest alternative, and what it protects.>
2. **<Lead phrase of the next.>** <Its reasoning. A named fallback is a decision too: say what would be switched to and what taking it would cost.>

## Considered options

- **<An option.>** Rejected: <the actual objection, so reopening starts from it>.
- **<An option.>** Deferred until <the condition that would make it worth deciding>.
- **<An option.>** Named fallback: <when it would be taken, and what taking it means>.

## Consequences

- **+** <What this makes easier or possible.>
- **−** <What this makes harder, rules out, or costs.>
- *Deferred, deliberately:* <what this ADR chose not to settle> — <where it will be settled: a document section, an issue, a later ADR>.

## Update (YYYY-MM-DD)

<Appended when reality moves: a later decision that narrows or extends this one, a gate that closed, a correction. The sections above are never edited.>
