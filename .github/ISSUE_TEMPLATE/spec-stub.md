---
name: Spec stub
about: One implementation beat, opened by the beats session for its own spec session to grill into a spec and tickets.
title: "spec: <beat name>"
labels: spec, spec:needs-grilling
---

<!--
The stub carries the beat's family label, `skill/<beat>`, which every
ticket under it carries too. The label is per beat, so no template can carry it: add
it when opening (`--label skill/<beat>`, or the sidebar).

The spec session moves `spec:needs-grilling` → `spec:ready-for-tickets` →
`spec:tickets-generated`; this body grows into the spec under the same headings.
-->

## Brief

A link to the reconciled design brief, and the section this beat comes from.

## Scope

What this beat covers, its boundaries, and what is explicitly out of it.

## Ready for tickets when

What is still open and has to be resolved before the spec is solid enough for tickets.
